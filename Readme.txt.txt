*** How to run the system ***

1. Open the file folder and run cmd.
2. In cmd type -- yarn install.
3. Open again new cmd type -- composer install.
4. Open again 3 new cmd then type 
	-- yarn watch,
	-- php artisan serve,
	-- yarn socket-server
